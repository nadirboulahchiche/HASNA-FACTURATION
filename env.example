# Configuration du serveur de licences HASNA
# Copier ce fichier en .env et remplir les valeurs

# URL de connexion PostgreSQL (fournie par Railway)
DATABASE_URL=postgresql://user:password@host:port/database

# Clé secrète pour les opérations admin (à générer, ex: un UUID)
ADMIN_KEY=votre-cle-admin-secrete-ici

# Port (Railway le définit automatiquement)
PORT=3000

# Environnement
NODE_ENV=production
